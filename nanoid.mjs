// Importar la función nanoid desde el paquete nanoid
import { nanoid } from 'nanoid';

// Generar dos IDs diferentes y guardarlos en variables
const id1 = nanoid(); // Genera un ID
const id2 = nanoid(); // Genera otro ID

// Imprimir los IDs en la consola
console.log('ID 1:', id1);
console.log('ID 2:', id2);
