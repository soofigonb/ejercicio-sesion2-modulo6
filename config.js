// Definimos una variable booleana
// const returnLength = true;
const returnLength = false; // Cambia a false si deseas obtener la concatenación

// Exportamos la variable
module.exports = { returnLength };
