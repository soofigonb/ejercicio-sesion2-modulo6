import RandExp from 'randexp';
export declare class RegExCache {
    regex: WeakMap<RegExp, RandExp>;
    string: Map<string, RandExp>;
    has(pattern: string | RegExp): boolean;
    set(pattern: string | RegExp, value: RandExp): WeakMap<RegExp, RandExp> | Map<string, RandExp>;
    get(pattern: string | RegExp, factory?: (instance: RandExp) => RandExp): RandExp;
}
