import type { ZodTypeAny } from '../internal/zod';
import type { Defaults } from './defaults';
import type { Context } from './generator';
import type { Transformer } from './transformer';
import { Utils } from './utils';
export declare class Runner {
    readonly transformer: Transformer;
    readonly defaults: Defaults;
    readonly utils: Utils;
    constructor(transformer: Transformer, schemaDefaults?: Partial<Defaults>);
    fromSchema<TSchema extends ZodTypeAny>(schema: TSchema, context?: Context): unknown;
    shouldHaveMatch(schema: ZodTypeAny, generator: unknown): asserts generator is NonNullable<unknown>;
}
